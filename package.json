{
  "dependencies": {
    "axios": "^1.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.5.0",
    "rimraf": "^5.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "scripts": {
    "cross": "npm run build && node build/make-a-polyanet-cross.js",
    "logo": "npm run build && node build/make-the-logo.js",
    "clear": "npm run build && node build/clear-map.js",
    "build": "rimraf ./build && tsc"
  }
}
